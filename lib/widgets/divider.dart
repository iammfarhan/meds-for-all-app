import 'package:flutter/material.dart';

class MedDivider extends StatelessWidget {
  const MedDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 0.5,
      color: Color(0x4A3C3C43),
    );
  }
}
